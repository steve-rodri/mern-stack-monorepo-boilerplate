{
  "name": "mern-stack-boilerplate",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^6.0.0",
    "eslint": "^7.20.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-config-prettier": "^7.2.0",
    "eslint-plugin-monorepo": "^0.3.2",
    "eslint-plugin-prettier": "^3.3.1",
    "lerna": "^3.22.1",
    "prettier": "^2.2.1"
  },
  "scripts": {
    "start": "lerna clean -y && lerna bootstrap && lerna run start --parallel"
  }
}
